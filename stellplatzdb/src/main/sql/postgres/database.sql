CREATE TABLE SEQUENCES (ID VARCHAR(255) NOT NULL, SEQUENCE_VALUE BIGINT, PRIMARY KEY (ID));
CREATE TABLE SURVEY (ID VARCHAR(30) NOT NULL, CREATED_AT TIMESTAMP WITH TIME ZONE, CREATED_BY VARCHAR(70), MODIFIED_AT TIMESTAMP WITH TIME ZONE, MODIFIED_BY VARCHAR(70), ACTIVE BOOL NOT NULL, DEFAULT_BIC INTEGER, DEFAULT_CITY VARCHAR(100), OPEN_FROM DATE NOT NULL, OPEN_UNTIL DATE NOT NULL, SURVEY_NAME VARCHAR(255) NOT NULL, OPTLOCK INTEGER, PRIMARY KEY (ID));
CREATE TABLE SURVEY_ENTRY (ID INTEGER NOT NULL, CREATED_AT TIMESTAMP WITH TIME ZONE, CREATED_BY VARCHAR(70), MODIFIED_AT TIMESTAMP WITH TIME ZONE, MODIFIED_BY VARCHAR(70), BIC INTEGER NOT NULL, CITY VARCHAR(100) NOT NULL, COUNTED_AT DATE NOT NULL, EMAIL VARCHAR(255), HOUSENR VARCHAR(30) NOT NULL, REMOTE_IP VARCHAR(100), STREET VARCHAR(255) NOT NULL, SUM_HOUSING_UNITS INTEGER, SUM_COURTYARD INTEGER, SUM_INSIDE INTEGER, USED_ABROAD_COURTYARD INTEGER, USED_ABROAD_INSIDE INTEGER, USED_LOCAL_COURTYARD INTEGER, USED_LOCAL_INSIDE INTEGER, VERIFIED_AT DATE, VERIFIED_BY DATE, OPTLOCK INTEGER, SURVEY_ID VARCHAR(30) NOT NULL, PRIMARY KEY (ID));
ALTER TABLE SURVEY_ENTRY ADD FOREIGN KEY (SURVEY_ID) REFERENCES SURVEY (ID) DEFERRABLE;
